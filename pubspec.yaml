name: spoutify
description: "spoutify"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  supabase_flutter: ^2.6.0
  provider: ^6.1.1
  http: ^1.2.1
  flutter_dotenv: ^5.1.0

  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.4
flutter:
  uses-material-design: true
  generate: true 

  assets:
    - .env